"use client"
import { useEffect, useState } from "react";

const Button = () => {
    return (
      <button className="h-12 rounded-lg bg-white font-bold px-5 text-black">Sign In</button>
    );
  };
  export default Button;